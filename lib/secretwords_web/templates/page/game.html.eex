<h2>Game: <%= @session_id %></h2>

<table>
  <%= for row <- Enum.chunk_every(@words, @grid_size) do %>
    <tr>
      <%= for word <- row do %>
        <td class="word"><%= word %></td>
      <% end %>
    </tr>
  <% end %>
</table>
