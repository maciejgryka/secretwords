defmodule SecretwordsWeb.UserVisitsHomepageTest do
  use SecretwordsWeb.FeatureCase, async: true

  # test "user can visit homepage", %{session: session} do
  #   session
  #   |> visit("/")
  #   |> assert_has(Query.css("h1", text: "Secretwords"))
  # end
end
